import { StateForUsers, UserDispatch } from "../interfaces/interface";

export const mainReducer = (
  state: StateForUsers,
  action: UserDispatch
): StateForUsers => {
  // const { type, payload } = action;

  return state;
};
